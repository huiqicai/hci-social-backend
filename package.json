{
  "name": "hci-social-backend",
  "version": "0.1.0",
  "description": "Backend/API layer for the UB CSE370 course project",
  "license": "MIT",
  "repository": {},
  "scripts": {
    "postinstall": "prisma generate",
    "build": "shx rm -rf build && tsc -p tsconfig.app.json",
    "start": "cross-env NODE_ENV=production node ./build/index.js",
    "develop": "shx mkdir -p public/uploads/files && npm run build && concurrently \"tsc -p tsconfig.app.json -w\" \"cross-env NODE_ENV=development supervisor -w ./build --no-restart-on error ./build/index.js\"",
    "build:test": "shx rm -rf build && tsc -p tsconfig.test.json",
    "start:test": "cross-env NODE_ENV=test mocha --file ./build/test.js \"./build/**/*.spec.js\"",
    "test": "npm run build:test && concurrently \"tsc -p tsconfig.test.json -w\" \"cross-env NODE_ENV=test mocha --file ./build/test.js -w \\\"./build/**/*.spec.js\\\"\"",
    "build:e2e": "shx rm -rf build && tsc -p tsconfig.e2e.json",
    "start:e2e": "cross-env NODE_ENV=e2e mocha --timeout 4000 --file ./build/e2e.js \"./build/e2e/**/*.js\"",
    "e2e": "npm run build:e2e && concurrently \"tsc -p tsconfig.e2e.json -w\" \"cross-env NODE_ENV=e2e mocha --file ./build/e2e.js -w \\\"./build/e2e/**/*.js\\\"\"",
    "lint": "eslint --ext ts src",
    "lint:fix": "eslint --ext ts --fix src",
    "reset-db": "npm run build && npx foal run reset-db",
    "push-schema": "npm run build && npx foal run push-schema",
    "make-migrations": "npm run build && npx foal run make-migrations",
    "migrate": "npm run build && npx foal run migrate"
  },
  "engines": {
    "node": ">=10"
  },
  "dependencies": {
    "@foal/core": "^2.8.1",
    "@foal/socket.io": "^4.2.0",
    "@foal/storage": "^2.8.1",
    "@foal/swagger": "^2.8.1",
    "@prisma/client": "^3.10.0",
    "@types/express": "^4.17.13",
    "@types/socket.io": "^3.0.2",
    "jsonwebtoken": "^8.5.1",
    "nodemailer": "^6.7.2",
    "shelljs": "^0.8.5",
    "source-map-support": "~0.5.21"
  },
  "devDependencies": {
    "@foal/cli": "^2.8.1",
    "@types/jsonwebtoken": "^8.5.8",
    "@types/mocha": "9.1.0",
    "@types/node": "17.0.21",
    "@types/nodemailer": "^6.4.4",
    "@types/shelljs": "^0.8.11",
    "@types/supertest": "^2.0.11",
    "@typescript-eslint/eslint-plugin": "~5.13.0",
    "@typescript-eslint/parser": "~5.13.0",
    "concurrently": "~7.0.0",
    "cross-env": "^7.0.3",
    "eslint": "^8.10.0",
    "mocha": "~9.2.1",
    "prisma": "^3.13.0",
    "shx": "^0.3.4",
    "supertest": "~6.2.2",
    "supervisor": "~0.12.0",
    "typescript": "~4.6.2"
  }
}
